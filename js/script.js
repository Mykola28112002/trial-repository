function getEvenNumbers(start, end) {
  const result = []
   // Change code below this line
  for (const number of getEvenNumbers)
    console.log(number);
    if (number % 2 === 0) {
      result.push(number);
    }
  return result;
    // Change code above this line
}
  
console.log(getEvenNumbers(1, 6))
